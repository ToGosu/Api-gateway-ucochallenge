# Reglas de exclusión antes de aplicar CRS
# Usar este archivo para excluir reglas específicas si causan falsos positivos

# Ejemplo: Excluir reglas para rutas específicas
# SecRule REQUEST_URI "@beginsWith /api/v1/cities" \
#     "id:900001,\
#     phase:1,\
#     pass,\
#     t:none,\
#     nolog,\
#     ctl:ruleRemoveById=942100"

